# Optional Lingua course audio

Place only audio recordings you own or are licensed to distribute here.

Suggested naming:
`audio/<language>/<level>-<unit>-<phrase>.mp3`

Then register each path in the `COURSE_AUDIO` map in `index.html`.

If a recording is missing, Lingua automatically uses the browser pronunciation engine instead.
